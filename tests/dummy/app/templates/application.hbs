<h2 id="title">Ember Filestack Demo</h2>

<button onclick={{action (mut showFilestack) true}}>Choose file</button>

{{#if showFilestack}}
  <FilestackPicker
    @accept="image/*"
    @onUploadDone={{action "fileSelected"}}
    @onClose={{action (mut showFilestack) false}}
  />
{{/if}}

{{#if fileHandle}}
  <h3>You picked:</h3>
  <img src={{filestack-url fileHandle}} alt="picked image">
{{/if}}

{{#if fileHandle}}
  <h3>Previewer:</h3>
  <FilestackPreview @handle={{fileHandle}}/>
{{/if}}